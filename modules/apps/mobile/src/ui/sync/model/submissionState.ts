/**
 * How far an answer has travelled towards the school, as the student sees it.
 *
 * The rule is the client library's, because the web cabinet answers the same
 * question about the same journal; this slice keeps the name the screens here
 * import it under.
 */

export { type SubmissionState, submissionStateOf, SubmissionStates } from '@vidya/client'

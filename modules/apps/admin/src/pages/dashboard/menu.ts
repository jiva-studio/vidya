import type { MenuGroup } from '@/shared/navigation'

export const menu: MenuGroup[] = [
  {
    label: 'nav-group-work',
    items: [{ route: 'dashboard', label: 'nav-dashboard', icon: 'home' }],
  },
]

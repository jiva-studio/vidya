export const pageClasses = ['flex', 'flex-col', 'gap-[var(--space-5)]']

export const formClasses = ['flex', 'flex-col', 'gap-[var(--space-5)]', 'max-w-[var(--form-max)]']

export const formLoadingClasses = ['flex', 'flex-col', 'gap-[var(--space-4)]']

// A draft stands beside the name rather than in a column of its own: most
// courses are published, and a column of one repeated word says nothing.
export const nameGroupClasses = ['flex', 'items-center', 'gap-[var(--space-2)]', 'min-w-0']

export const nameClasses = ['truncate']

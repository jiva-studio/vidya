// The re-export of @vidya/ui lives here, so a component import never names the
// package twice and swapping the library is one file.
//
// The package itself is still being built on its own branch; this barrel is
// filled the moment it lands.
export {}

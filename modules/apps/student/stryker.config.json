{
  "$schema": "../../node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "packageManager": "npm",
  "testRunner": "command",
  "commandRunner": { "command": "npx vitest run --exclude '**/contentSecurityPolicy.spec.ts'" },
  "coverageAnalysis": "off",
  "incremental": true,
  "incrementalFile": "../../../.stryker/incremental/student.json",
  "mutate": [
    "src/**/*.ts",
    "!src/**/__tests__/**",
    "!src/**/*.spec.ts",
    "!src/**/*.stories.ts",
    "!src/**/index.ts",
    "!src/**/types.ts",
    "!src/**/styles.ts",
    "!src/app/main.ts",
    "!src/shared/testing/**"
  ],
  "reporters": ["clear-text", "progress"],
  "clearTextReporter": { "maxTestsToLog": 0 },
  "timeoutMS": 120000,
  "timeoutFactor": 2,
  "dryRunTimeoutMinutes": 15,
  "concurrency": 4,
  "thresholds": { "high": 80, "low": 60, "break": 50 }
}

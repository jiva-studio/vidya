/** What a catalogue, a course and their rows are drawn with. */
export const listClasses = ['flex flex-col gap-[var(--space-3)]']

export const cardClasses = [
  'flex flex-col items-start gap-[var(--space-2)]',
  'rounded-[var(--radius-md)] border border-[var(--color-border)] p-[var(--space-4)]',
]

export const headingClasses = ['text-[length:var(--text-lg)] font-medium hover:underline']

export const rowClasses = [
  'flex items-baseline justify-between gap-[var(--space-3)]',
  'border-b border-[var(--color-border)] py-[var(--space-3)]',
]

export const numberClasses = ['text-[var(--color-text-muted)] tabular-nums']

export const placeClasses = ['flex flex-col items-start gap-[var(--space-2)]']

// A link that offers an action has to look like one: inside a card of plain
// sentences an unstyled anchor reads as another sentence.
export const noticeClasses = [
  'flex flex-col items-start gap-[var(--space-2)]',
  'rounded-[var(--radius-md)] border border-[var(--color-border)] p-[var(--space-4)]',
]
export const formClasses = ['flex flex-col gap-[var(--space-5)]']

export const fieldClasses = ['flex flex-col gap-[var(--space-2)] border-0 p-0']

export const choiceListClasses = ['flex flex-col gap-[var(--space-2)]']

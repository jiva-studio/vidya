export const panelClasses = ['flex w-full flex-col items-start gap-[var(--space-4)]']

export const schoolClasses = ['flex items-center gap-[var(--space-3)]']

export const schoolNameClasses = ['text-[length:var(--text-xl)] font-semibold']
